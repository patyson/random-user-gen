//This is the sticky header with user settings, and maybe some global UI
import '../style/header.scss';

const Header = () => {
    return (
        <header>
            <div className="profile"></div>
        </header>
    )
}

export default Header
